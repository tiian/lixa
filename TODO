now client and server can goes maintenance mode, but there is not a way to retrieve the list of recovery failed transactions: only the next transaction to be recovered is passed from server to client.
The issue is complicated by multithread behavior of the server: the list should be produced merging the content of all the threads..... A good idea is necessary to go on... ;)

go on with bug 2983464 fixing...

Remove all @@@ from source code: every one was/is an "open point"

